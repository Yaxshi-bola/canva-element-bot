services:
  - type: web
    name: canva-element-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: NODE_VERSION
        value: 18.0.0
